# Plan: 01-01 - Foundation, Auth, and DB

**Goal**: Initialize the Next.js project, configure Auth.js with domain restriction, and set up Prisma with Supabase.

<frontmatter>
wave: 1
depends_on: []
files_modified:
  - package.json
  - prisma/schema.prisma
  - .env
  - src/auth.ts
  - src/app/api/auth/[...nextauth]/route.ts
  - src/middleware.ts
autonomous: true
</frontmatter>

<tasks>
<task id="T1">
<read_first>
- .planning/phases/01-foundation-auth/01-CONTEXT.md
- .planning/phases/01-foundation-auth/01-RESEARCH.md
</read_first>
<action>
Initialize a new Next.js 16 project in the current directory using `npx create-next-app@latest .` with the following choices:
- TypeScript: Yes
- ESLint: Yes
- Tailwind CSS: Yes
- `src/` directory: Yes
- App Router: Yes
- Import alias: Yes (@/*)
</action>
<acceptance_criteria>
- `package.json` exists in the root.
- `src/app/layout.tsx` exists.
- `next.config.js` or `next.config.ts` exists.
</acceptance_criteria>
</task>

<task id="T2" depends_on="T1">
<read_first>
- package.json
</read_first>
<action>
Install dependencies:
- Auth.js: `npm install next-auth@beta`
- Prisma: `npm install @prisma/client` and `npm install prisma --save-dev`
- shadcn/ui: `npx shadcn@latest init`
</action>
<acceptance_criteria>
- `node_modules` contains `next-auth`, `@prisma/client`.
- `components.json` exists (from shadcn/ui init).
</acceptance_criteria>
</task>

<task id="T3" depends_on="T2">
<read_first>
- .planning/phases/01-foundation-auth/01-RESEARCH.md
</read_first>
<action>
Set up Prisma and Supabase:
- Run `npx prisma init`.
- Update `.env` with the Supavisor Transaction connection string (user must provide this later, but we will scaffold it).
- Define models in `prisma/schema.prisma`: `User`, `Item`, `Zone`, `Report`.
- Focus on the `signIn` domain restriction logic in the `auth.ts` config.
</action>
<acceptance_criteria>
- `prisma/schema.prisma` contains `User` and `Item` models.
- `.env` contains `DATABASE_URL`.
</acceptance_criteria>
</task>

<task id="T4" depends_on="T3" autonomous="false">
<read_first>
- prisma/schema.prisma
</read_first>
<action>
[BLOCKING] Ask the user for the Supabase Connection String and run:
`npx prisma db push`
</action>
<acceptance_criteria>
- Command output shows "Your database is now in sync with your Prisma schema".
</acceptance_criteria>
</task>

<task id="T5" depends_on="T1">
<read_first>
- .planning/phases/01-foundation-auth/01-CONTEXT.md
</read_first>
<action>
Configure Auth.js:
- Create `src/auth.ts` in root.
- Implement `EmailProvider` or `NodemailerProvider` for magic links.
- Implement the `signIn` callback to restrict domain to `*@*.nits.ac.in`.
- Create the App Router handler in `src/app/api/auth/[...nextauth]/route.ts`.
- Create `src/middleware.ts` to protect `/dashboard`.
</action>
<acceptance_criteria>
- `auth.ts` contains domain check logic.
- `middleware.ts` redirects unauthenticated users to `/api/auth/signin`.
</acceptance_criteria>
</task>
</tasks>

## Verification Criteria

### Automated
- `npm run build` exits 0.
- `npx prisma validate` exits 0.

### Manual
- User can visit the site and sees the Next.js welcome or login page.
- Attempting to access `/dashboard` without login redirects correctly.
- Attempting to login with a non-institute email returns an error.

---
*Created: 2026-04-15*
