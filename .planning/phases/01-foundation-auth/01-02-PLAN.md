# Plan: 01-02 - UI Components and Layout

**Goal**: Establish the visual identity and layout for the Smart Lost & Found platform.

<frontmatter>
wave: 2
depends_on: ["01-01-PLAN.md"]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/navbar.tsx
  - src/components/footer.tsx
  - src/app/globals.css
autonomous: true
</frontmatter>

<tasks>
<task id="T1">
<read_first>
- .planning/phases/01-foundation-auth/01-CONTEXT.md
- components.json
</read_first>
<action>
Customize the shadcn/ui theme and Tailwind config:
- Update `src/app/globals.css` with a premium dark-mode-first color palette (e.g., deep slates and vibrant accents).
- Set up a standard Responsive Layout with Nav, Sidebar (for dashboard), and Responsive Mobile Menu.
</action>
<acceptance_criteria>
- `src/app/globals.css` contains the custom theme variables.
- The layout includes a `<Navbar />` and `<Footer />`.
</acceptance_criteria>
</task>

<task id="T2" depends_on="T1">
<read_first>
- src/app/page.tsx
</read_first>
<action>
Develop the Landing Page:
- Hero section: "Smart Lost & Found" with a clear CTA ("Join as Finder" / "Sign In").
- Feature sections: Visualize the "Intelligent Matching" (Fuse.js + Grid) and "Tiered Verification" concepts.
- Use shadcn `Card` and `Button` components.
</action>
<acceptance_criteria>
- `src/app/page.tsx` renders without errors.
- Visual elements (Card, Hero) are present and responsive.
</acceptance_criteria>
</task>

<task id="T3" depends_on="T2">
<read_first>
- src/app/layout.tsx
</read_first>
<action>
Next.js 16 Integration check:
- Ensure metadata and fonts (e.g., Geist or Outfit) are optimized.
- Verify `next/dynamic` usage for larger components if necessary.
</action>
<acceptance_criteria>
- Performance score (Lighthouse or manual check) is high.
- Correct fonts are applied.
</acceptance_criteria>
</task>
</tasks>

## Verification Criteria

### Automated
- `npm run dev` starts without errors.
- `npx shadcn@latest check` passes.

### Manual
- Navigate through the landing page on mobile and desktop.
- Verify the styling matches the "premium" aesthetic requested in PROJECT.md.

---
*Created: 2026-04-15*
